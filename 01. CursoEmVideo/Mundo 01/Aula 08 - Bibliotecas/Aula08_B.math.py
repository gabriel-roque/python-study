from math import sqrt
valor = int(input('Insira o valor: '))

#quando for utilizar apenas uma função do pacote, não há necessidade de utilizar o .MATH
raiz = sqrt(valor)
print('Raiz: {}'.format(raiz))